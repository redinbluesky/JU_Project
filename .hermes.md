# JU Project 공통 규칙

## 단일 기준

1단계 범위와 완료 조건은 `docs/10-scope/1단계-기술-프로토타입-목표-범위-인수조건.md`를 따른다. 원본 요구사항은 `docs/00-input/원본-요구사항-RFP.md`다. 두 문서가 충돌하면 승인된 1단계 범위 문서가 우선한다.

## 소유권

| 경로 | 작성 권한 | 읽기·검증 |
|---|---|---|
| `.hermes.md` | 사용자 승인 후 `delivery-lead` | 모든 BOT |
| `README.md` | `delivery-lead` | 모든 BOT |
| `.gitignore` | `local-builder`; 구조 변경은 사용자 승인 | 모든 BOT |
| `docs/00-input/**` | 수정 금지 | 모든 BOT |
| `docs/10-scope/**` | 사용자 승인 후 `delivery-lead` | 모든 BOT |
| `docs/20-design/UX-화면-명세.md` | `ux-designer` | 나머지 BOT |
| `docs/20-design/디자인-토큰.md` | `ux-designer` | 나머지 BOT |
| `docs/20-design/시스템-설계.md` | `delivery-lead` | `local-builder`, `quality-gate` |
| `docs/20-design/데이터-모델.md` | `local-builder` | `delivery-lead`, `quality-gate` |
| `docs/30-delivery/**` | `delivery-lead` | 나머지 BOT |
| `docs/40-quality/**` | `quality-gate` | 나머지 BOT |
| `app/**` | `local-builder` | 나머지 BOT |
| `runtime/**` | 실행 중인 BOT | `quality-gate` |

다른 BOT의 소유 파일은 직접 수정하지 않고 소유자에게 변경을 요청한다. `quality-gate`는 `app`을 읽고 실행할 수 있지만 수정하지 않는다. 사용자는 범위 변경, 보호 문서 변경과 최종 승인 권한을 가진다.

## 보호 문서

- `.hermes.md`, `docs/00-input/**`, `docs/10-scope/**`를 보호 문서로 취급한다.
- `docs/00-input/**`는 원본 보존 영역이므로 수정하지 않는다.
- 보호 문서 변경 필요성은 `delivery-lead`가 영향 분석 후 사용자에게 상신한다.
- 사용자 승인 후 `delivery-lead`만 보호 문서를 수정한다.

## 작업 선점

- 모든 쓰기 작업은 `delivery-lead`가 배정한 작업번호와 수정 허용 경로를 가져야 한다.
- `delivery-lead`만 `docs/30-delivery/진행현황.md`의 활성 선점표를 수정한다.
- BOT은 활성 선점표에 자신의 작업이 등록된 것을 확인한 뒤 수정한다.
- 활성 선점과 경로가 겹치면 새 작업을 시작하지 않는다.
- 하나의 파일에는 동시에 한 BOT만 쓰기 권한을 가진다.
- 문서는 파일 단위, 애플리케이션은 파일 또는 명확한 하위 경로 단위로 선점한다.
- 여러 파일을 수정할 작업은 시작 전에 예상 경로를 함께 등록한다.
- 배정되지 않은 파일이 필요하면 수정을 중지하고 `delivery-lead`에게 선점 범위 확장을 요청한다.
- 읽기와 검토는 허용하지만 작성 중인 결과를 기준으로 최종 검증하지 않는다.

## 작업 완료와 인계

- 담당 BOT은 변경 파일, 실행한 검증, 실제 결과, 남은 위험을 보고한다.
- `delivery-lead`가 보고를 확인한 뒤 선점을 해제한다.
- `quality-gate`는 선점이 해제된 결과만 최종 검증한다.
- 실패하거나 중단된 작업도 변경 파일과 현재 상태를 보고한 뒤 선점을 해제한다.
- 선점 해제 없이 다른 BOT이 작업을 이어받지 않는다.

## 충돌 처리

- 작업 시작 직전에 대상 파일의 현재 내용을 다시 읽는다.
- 읽은 뒤 예상하지 못한 변경을 발견하면 덮어쓰지 않고 작업을 중지한다.
- 같은 파일의 상충된 변경은 `delivery-lead`가 기준 문서와 소유권에 따라 조정한다.
- 범위 또는 사용자 판단이 필요한 충돌은 사용자에게 상신한다.
- 충돌 해결 전에 양쪽 변경의 의도와 검증 결과를 보존한다.

## 강제 해제

- 선점은 시간만으로 자동 만료하지 않는다.
- BOT 응답이 중단되면 `delivery-lead`가 작업 상태와 변경 파일을 확인한다.
- 확인 후 작업을 `중단`으로 기록한 경우에만 다른 BOT에 다시 배정한다.
- 사용자만 모든 선점을 즉시 해제하거나 소유권을 변경할 수 있다.

## 변경 요청 형식

다른 BOT 소유 파일의 변경이 필요하면 다음 정보를 소유자와 `delivery-lead`에게 전달한다.

```text
작업번호:
요청자:
대상 소유자:
대상 파일:
필요한 변경:
변경 이유:
관련 인수 조건:
차단 여부:
```

## 공통 작업 규칙

- 범위 변경은 사용자 승인 후 기준 문서에 반영한다.
- BOT별 코드나 문서 복사본을 만들지 않는다.
- 생성된 DB·PDF·엑셀은 `runtime/`에 저장한다.
- 기술 스택 확정 전 `app` 내부를 임의로 구성하지 않는다.
- 변경 전 관련 흐름과 기존 문서를 읽고 단일 책임 위치를 수정한다.
- 완료 보고에는 변경 파일, 실행한 검증, 실제 결과, 남은 위험을 포함한다.
- `quality-gate`의 독립 인수 검증 전에는 프로젝트 완료를 선언하지 않는다.

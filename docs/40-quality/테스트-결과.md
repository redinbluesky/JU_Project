# WP-13 테스트 결과 — 자동화·핵심 불변조건·QA-001~040 인수조건 판정

- 작업: WP-13-A (자동화 테스트 + 핵심 불변조건 9건) + WP-13-B (QA-001~040 판정,
  UI/브라우저 검증, 최종 출시 판정)
- 담당: quality-gate
- 대상: D:/llm/JU_Project (app/** 읽기·실행만, 미수정)
- 판정 범위: 전체 자동화 테스트 결과, 상태 전이/이력, 실패 시 부분저장 방지,
  StaleDataError 409, PDF 권한·표시, 필터 공유, XLSX 10열·정합성·수식 0,
  SQLite 재오픈 보존, 개인정보·자격증명 미사용.
- 화면/UI 항목: **제4·6장에서 WP-14-D로 최종 판정 완료** (자동화·브라우저 증거 반영).

---

## 1. 실행 환경과 명령

```
uv run pytest tests/ -v
```

- CWD: D:/llm/JU_Project
- **WP-14-D 실제 결과: 190 passed, 4 warnings in 3.21s** — 실패 0건, 오류 0건.
- 신규 UI/에러 회귀까지 포함한 전체 190개 테스트를 재실행했다.
- 경고 4건: starlette/fastapi testclient 내부 deprecation(httpx, HTTP_422 상수명) —
  기능 영향 없는 비차단 잔여 위험.
- 기존 WP-13 기준(121 passed)은 WP-14-D에서 190 passed로 갱신한다.

## 2. 테스트 파일별 통과 수 (WP-14-D)

| 파일 | 통과 |
|---|---:|
| test_error_responses.py | 6 |
| test_excel_download.py | 19 |
| test_excel_export.py | 14 |
| test_integration_artifacts.py | 4 |
| test_integration_flow.py | 1 |
| test_pdf_download.py | 12 |
| test_request_counter.py | 3 |
| test_request_create.py | 5 |
| test_request_query.py | 14 |
| test_request_update.py | 10 |
| test_request_validation.py | 11 |
| test_statistics.py | 10 |
| test_status_transition.py | 8 |
| test_status_transition_api.py | 10 |
| test_ui_form.py | 21 |
| test_ui_list.py | 22 |
| test_ui_detail.py | 13 |
| test_ui_dashboard.py | 7 |
| **합계** | **190** |

- 실제 pytest 수 기준 UI 테스트 파일별 통과: form 21, list 22, detail 13, dashboard 7이다.
- 전용 에러 응답 테스트는 6개이며, 전체 회귀 합계 190개와 일치한다.

## 3. 핵심 불변조건 9건 — 판정과 증거

### ① 상태 순차 전이 (RECEIVED→PICKED_UP→DISINFECTED→DELIVERED)
**판정: 통과**
- 증거: tests/test_status_transition.py::TestAllowedTransitions(3건),
  tests/test_integration_flow.py(전체 흐름 단일 테스트, 이력 sequence 1~4·상태 4단 확인)
- 구현: app/main/services/request_service.py ALLOWED_TRANSITIONS(108) 기반 단일 전이.

### ② 건너뛰기·역방향·중복·배송완료 이후 변경 거부
**판정: 통과**
- 증거: tests/test_status_transition.py::TestRejectedTransitions
  (skip_disinfected_from_received, backward_picked_up_to_received,
  same_status_duplicate_rejected, after_delivered_rejected, nonexistent_request_id_rejected)
- API 수준 409: tests/test_status_transition_api.py
  (skip/reverse/duplicate/after_delivered/missing_id 모두 409)
- 거부 시 이력 미추가도 함께 검증됨.

### ③ 실패 시 상태·이력·카운터 부분 저장 방지
**판정: 통과**
- 증거:
  - 검증 실패(422) 시 저장 없음: test_integration_flow.py step8
    (전체 수량 0 → 422, Request 수 불변, 카운터 1 유지)
  - PATCH 검증 실패 시 무변경: test_request_update.py::TestPatchValidation422
    (test_patch_validation_failure_does_not_modify)
  - 카운터 롤백: test_request_counter.py::TestCounterRollbackOnFailure
  - StaleDataError 시 이력 미추가: test_status_transition_api.py
    ::TestStaleDataError(409 + 세션 정리 확인)

### ④ StaleDataError 409 처리 및 자동 재시도 없음
**판정: 통과**
- 증거:
  - tests/test_status_transition_api.py::TestStaleDataError
    (test_stale_data_error_returns_409)
  - 구현: app/main/api/requests.py 91-96 — StaleDataError를 잡아 rollback 후
    409("다시 조회 후 재시도해 주세요") 반환. 코드에 자동 재시도 로직 없음.
- 비고: 실제 2세션 동시 쓰기 경쟁(경쟁 조건부) 테스트는 없음.
  설계 10.1 "단일 프로세스·단일 쓰기" 전제 하에서 허용, 위험으로 관리(결함-목록 2.1).

### ⑤ PDF 상태 권한(소독완료 이전 거부/이후 허용) + 프로토타입 표시
**판정: 통과**
- 증거:
  - 거부: tests/test_pdf_download.py (RECEIVED/PICKED_UP → 409)
  - 허용: tests/test_pdf_download.py (DISINFECTED/DELIVERED → 200,
    application/pdf, %PDF 헤더, 크기>0, 파일명 {request_no}.pdf)
  - 프로토타입 표시: tests/test_integration_artifacts.py::test_pdf_integration
    (PDF Title 메타에 PROTOTYPE_NOTICE UTF-16BE 포함 확인)
  - 구현: app/main/api/requests.py download_request_pdf(180-208)
    — 요청 시점에 DB 재조회 후 DISINFECTED/DELIVERED만 허용.
    app/main/services/pdf_service.py — 상단 PROTOTYPE_NOTICE + /Title 메타 기록.

### ⑥ 목록·통계·엑셀 공통 필터 (pickup_date 기간 + 사업소 + 상태)
**판정: 통과 (API 수준)**
- 증거:
  - 목록: tests/test_request_query.py (14건; 기간/사업소/상태/조합/무결)
  - 통계: tests/test_statistics.py (10건; TestConsistencyWithListApi 포함,
    동일 필터 total 일치, from>to 422)
  - 엑셀: tests/test_excel_download.py (19건; 동일 파라미터,
    TestDateRangeOnPickupDate 등)
  - 통합 대조: tests/test_integration_artifacts.py::test_statistics_list_consistency
    (목록 total = 통계 total_requests, 수량 합계 일치, 상태 필터 동일 행)
  - 구현: app/main/api/requests.py — 세 엔드포인트 모두 동일한
    pickup_date_from/pickup_date_to/business_office_id/current_status 쿼리 인수.
- 비고: 목록/통계/엑셀 화면 및 다운로드 버튼 흐름도 WP-14-D UI 자동화·브라우저에서 확인.

### ⑦ XLSX 10열 고정 · DB 정합성 · formula 셀 0개
**판정: 통과**
- 증거:
  - 필수 10 헤더 정확한 순서 + 행 수 = 목록:
    tests/test_integration_artifacts.py::test_xlsx_db_consistency
    (EXPECTED_HEADERS 10개, openpyxl 재로드, DB 행값 대조)
  - DB 정합: 같은 테스트에서 pickup_date ASC·id ASC 정렬로 DB 원본과 셀값 대조.
  - 수식 인젝션: tests/test_excel_download.py::TestFormulaInjection
    (injection_addresses_stored_as_string, no_formula_cells_anywhere —
    data_type=='f' 셀 0개), tests/test_excel_export.py::TestOpenpyxlFormulaPrevention
    (=,+,−,@ 시작 문자열 4종 sanitize)
  - 완료일 비어 있음 → 빈 셀(None), DELIVERED만 완료일:
    test_excel_download.py::TestValuesMatchDb
- 비고: 목록 화면의 엑셀 다운로드 링크와 브라우저 경로는 WP-14-D UI 테스트로 확인.

### ⑧ SQLite 재오픈 후 데이터·이력 보존
**판정: 통과**
- 증거: tests/test_integration_artifacts.py::test_sqlite_reopen_preserves_state
  - 실제 파일 기반 임시 .db(tmp_path/integration.db)
  - DELIVERED 전이 → 기존 session.close() → 새 engine/session 재오픈
  - Request 1건 / DELIVERED / completion_date 불변 / history 4건 sequence [1,2,3,4]
  - sidecar(-wal/-shm) 정리 확인.

### ⑨ 개인정보·자격증명 미사용
**판정: 통과**
- 근거:
  - tests/, app/, docs/40-quality/ 전체에서 KR 휴대폰 패턴(010~019 계열),
    이메일 패턴, 자격증명 값 스캔 → 0건.
  - 테스트 픽스처는 합성값 사용 (예: "서울시 강남구 테헤란로 123", "서울사업소").
  - runtime/db/ju-project.db 내용 확인: 3개 시드 사업소 + 테스트 생성 2건
    (주소 "수정 후 주소" / "상태 API 직접 검증") — 실제 개인정보 없음.
  - app 코드에서 자격증명/비밀정보 참조 없음.
- 비고: 보고서에는 실제 개인정보 값 자체를 복제하지 않음(합성값 예시만 인용).

## 4. QA-001~040 인수조건 최종 판정 (WP-14-D)

판정 기준:
- **통과**: 실제 자동화 테스트 또는 브라우저/코드 증거가 있는 항목
- **조건부**: API/자동화 일부만 검증되어 화면 또는 사용자 확인이 남은 항목
- **미검증**: 현재 증거 범위에서 확인할 수 없는 항목
- **실패**: 요구사항 위반 증거 — **0건**

UI/브라우저 검증 전제 (4.1절): WP-14-D에서 화면 라우트·정적 자산·PC Chrome·모바일 375px·접근성 속성을 실제 검증했다. 따라서 화면 항목을 미검증으로 남기지 않고 자동화/브라우저 증거에 따라 판정한다.

### 4.1 화면/브라우저 검증 범위와 실제 결과 (WP-14-D)

- 화면 라우트: `/requests/new`, `/admin/requests`, `/admin/requests/1`, `/admin/dashboard`.
- PC Chrome 실측: 네 화면 모두 **HTTP 200**. Wanted CSS(`4f34aaf` 기준 원본),
  앱 CSS/JS 및 토큰 정적 자산도 **HTTP 200**.
- 접근성 실측: 네 화면에서 `role="status"` 및 `aria-live="polite"` 확인.
  UI 자동화도 form/list/detail/dashboard에서 동일 속성과 레이블·상태 배지를 검증했다.
- 모바일 375px 실측: 네 화면 모두 `clientWidth=375`, `scrollWidth=375`,
  가로 overflow 없음.
- 화면 내용: 접수폼 필드·레이블·제출 버튼, 목록 필터/엑셀/상세 링크/상태 badge,
  상세 이력·다음 상태 버튼·소독완료 PDF 버튼, 대시보드 통계·기간 필터·empty state 확인.
- 비차단 관측: `/favicon.ico` **404**.
- 따라서 기존 QA-033~037 화면 미검증 조건은 해소되었으나, 아래 PPT는 실제 사용자
  수동 인수 결과가 아니라 실행용 가이드임을 별도로 구분한다.

### 4.2 QA-001~040 판정표

| QA | 항목 | 판정 | 증거 |
|----|------|------|------|
| QA-001 | 접수 생성 정상 | 통과 | test_request_create.py: 201, request_no=R-0000000001, RECEIVED, created_at 기록, DB 1건 |
| QA-002 | 접수 생성 — 다른 사업소 | 통과 | test_request_query.py: OFFICE_A(id 1)/OFFICE_B(id 2) 시딩, 두 사업소에 각각 접수 생성 성공 + 사업소 필터 대조. create 픽스처도 사업소 3개 시딩 |
| QA-003 | 접수 생성 — 자택 | 통과 | test_request_create.py: pickup_location_type=HOME(자택) 저장 검증 |
| QA-004 | 접수 생성 — 여러 품목 | 통과 | test_request_create.py: 전동침대 1/휠체어 0/기타 2, 전 필드 DB 저장 검증 (test_all_fields_persisted) |
| QA-005 | 수량 0 거부 | 통과 | test_request_validation.py: ValidationError + DB 0건 |
| QA-006 | 음수 수량 거부 | 통과 | test_request_validation.py: 전동침대 -1, 휠체어 -1 각각 422(ValidationError) |
| QA-007 | 정수 아님 거부 | 통과 | test_request_validation.py: 1.5, 문자열 수량 거부 |
| QA-008 | 과거 날짜 거부 | 통과 | test_request_validation.py: 어제/오늘 거부 (test_reject_yesterday, test_reject_today) |
| QA-009 | 필수값 누락 거부 | 통과 | test_request_validation.py: business_office_id/pickup_date/address 누락 각각 거부 |
| QA-010 | 접수→수거완료 | 통과 | test_status_transition_api.py: 200, PICKED_UP, 이력 2건, changed_at 기록 |
| QA-011 | 수거→소독완료 | 통과 | test_status_transition_api.py + test_integration_flow.py: DISINFECTED, 이력 3건 |
| QA-012 | 소독완료→PDF | 통과 | API + 화면 경로 검증: test_pdf_download.py 200, %PDF, PROTOTYPE 표시 + 상세 화면 PDF 버튼 확인 |
| QA-013 | 소독→배송완료 | 통과 | test_status_transition_api.py: DELIVERED, completion_date 기록, 이력 4건 |
| QA-014 | 전체 흐름 | 통과 | test_integration_flow.py: 4단계 전이 + 각 변경 시각 + 이력 sequence 1~4 |
| QA-015 | 건너뛰기 거부 | 통과 | test_status_transition_api.py: 409, 이력 미추가 |
| QA-016 | 역방향 거부 | 통과 | test_status_transition_api.py: 409, 상태/이력 불변 |
| QA-017 | 배송완료 이후 거부 | 통과 | test_status_transition_api.py: 409, 이력 불변 |
| QA-018 | 중복 전환 이력 중복 방지 | 통과 | test_status_transition_api.py::test_same_status_duplicate_409: 동일 전환 재요청 시 409 + 이력 수 불변 |
| QA-019 | 변경 시각 정확성 | 통과 | test_request_create.py: created_at==updated_at (초기), changed_at is not None. test_status_transition_api.py에서 전이 후 changed_at 기록 검증 |
| QA-020 | 재시작 후 데이터 보존 | 통과 | test_integration_artifacts.py::test_sqlite_reopen_preserves_state (session.close 후 새 engine 재접속 시 데이터 보존) |
| QA-021 | SQLite 파일 존재 | 통과 | `ls runtime/db/ju-project.db` 존재 확인 + `app/main/db/session.py` DB_PATH 정의 확인 |
| QA-022 | create-drop 없음 | 통과 | `app/main/db/session.py` 확인: create_engine(sqlite:///runtime/db/ju-project.db), create_all 없음, Base.metadata 사용만. `grep create_all app/` 결과 0건 |
| QA-023 | 기간 필터 | 통과 | test_request_query.py: pickup_date_from/to 필터, API/DB 대조 |
| QA-024 | 사업소 필터 | 통과 | test_request_query.py: business_office_id 필터, DB 대조 |
| QA-025 | 상태 필터 | 통과 | test_request_query.py: current_status 필터, DB 대조 |
| QA-026 | 복합 필터 | 통과 | test_request_query.py::test_combined_filter: 기간+사업소+상태 복합 |
| QA-027 | 통계 — 기간별 건수 | 통과 | test_statistics.py: total_requests + DB 집계 대조 |
| QA-028 | 통계 — 사업소/상태/품목 | 통과 | test_statistics.py: by_business_office, by_status, quantities, DB 대조 |
| QA-029 | 엑셀 기본 | 통과 | test_excel_download.py: 200, .xlsx, 헤더 10개, 행 수=목록, DB 셀값 대조 (TestBasicDownload/TestHeaderColumns/TestValuesMatchDb) |
| QA-030 | 엑셀 — 필터 후 다운로드 | 통과 | test_excel_download.py: 상태/사업소/기간 필터 후 export, DB 대조 (TestFilterConsistency) |
| QA-031 | 엑셀 — 수식 주입 방지 | 통과 | test_excel_download.py::TestFormulaInjection: =,+,−,@ 시작 주소가 텍스트 저장, formula 셀 0개. test_excel_export.py::TestOpenpyxlFormulaPrevention: 4종 sanitize |
| QA-032 | 엑셀 — 빈 결과 | 통과 | test_excel_download.py::TestZeroResult: 존재 안 하는 상태 필터 → 유효한 .xlsx, 헤더 10개, 데이터 0행 |
| QA-033 | PC Chrome 전체 흐름 | 통과 | 실제 PC Chrome에서 네 화면 HTTP 200 및 주요 흐름/콘텐츠 확인 + UI 자동화/통합 테스트 |
| QA-034 | 모바일 가로 스크롤 | 통과 | 실제 375px 네 화면 clientWidth=scrollWidth=375, overflow 없음 + dashboard 반응형 테스트 |
| QA-035 | 레이블·오류 메시지 | 통과 | 화면 레이블·오류 live status 확인 + test_request_validation.py 및 error response 테스트 |
| QA-036 | 색상 외 상태 표현 | 통과 | 목록/상세의 상태 텍스트·badge 확인, detail history·버튼 확인 |
| QA-037 | aria-live 속성 | 통과 | 네 화면 role="status" + aria-live="polite" 실제 확인 및 UI 테스트 4개 파일 증거 |
| QA-038 | 소독완료 PDF 다운로드 | 통과 | API + 상세 화면 버튼 검증 (test_pdf_download.py 12건, application/pdf, PROTOTYPE 표시) |
| QA-039 | 소독완료 이전 PDF 거부 | 통과 | test_pdf_download.py: RECEIVED/PICKED_UP → 409, 이력 불변 |
| QA-040 | PDF — 서버 상태 재확인 | 통과 | app/main/api/requests.py download_request_pdf: 요청 시점 DB 재조회 후 DISINFECTED/DELIVERED만 허용. test_pdf_download.py에서 검증 |

### 4.3 판정 요약

- 통과: **40건** (QA-001~040)
- 조건부: 0건
- 미검증: 0건
- 실패: 0건
- **합계: 40건**

## 5. 결함·관측 사항 (WP-14-D)

- 확인된 기능 결함: **0건**.
- 기존 500 예외 메시지 노출: WP-14-A 수정 후 `tests/test_error_responses.py` 7개 및
  전체 190개 회귀 통과 — **Fixed / Verified**.
- 비차단 관측: `/favicon.ico` 404, deprecation 경고 4건, runtime 테스트 산출물.
- 사용자 수동 인수 미완료: PPT는 17슬라이드 실행용 가이드이며 결과 증적이 아니다.

## 6. 최종 출시 판정 (WP-14-D)

### 6.1 판정 근거

1. **핵심 데이터 무결성**: 통과 — 전체 190개 테스트, 상태 전이/이력/부분저장 방지,
   PDF/XLSX 정합성, SQLite 보존.
2. **보안/개인정보**: 통과 — PII/자격증명 0건, 500 예외 원문 노출 수정 검증.
3. **QA-001~040**: **40 통과 / 조건부 0 / 미검증 0 / 실패 0**.
4. **UI/브라우저**: PC Chrome 네 화면 200, 정적 자산 200, role/status·aria-live,
   모바일 375px overflow 없음 및 화면 콘텐츠 확인.
5. **PPT**: 런타임 산출물 17슬라이드, 190개 테스트 수·조건부 가능 표기·참조 노트/부록
   포함. 실제 사용자 테스트 결과 기록용 가이드라는 문서 성격을 구분함.

### 6.2 최종 판정

**판정: 조건부 가능 (Conditional Release)**

- 기술적 자동화·브라우저 검증만 기준으로는 **출시 가능 수준**이다.
- 그러나 사용자 수동 인수 결과가 아직 없고 PPT도 실행용 가이드이므로,
  quality-gate 최종 판정은 보수적으로 **조건부 가능**을 유지한다.
- 사용자 결정: PPT에 따라 실제 인수 테스트를 실행·기록한 뒤 출시 가능으로 격상할지 승인.

## 7. WP-13 기준 대비 변경 요약

- 전체 테스트: 121 → 190 passed.
- QA-033~037: 조건부/미검증 → **모두 통과**.
- QA-012/038: 조건부 → **통과**(상세 화면 PDF 버튼 포함).
- 500 예외 메시지 노출: 관측 → **Fixed / Verified**.
- UI 미구현 위험: 해소(사용자 수동 인수는 별도 위험).
# 결함 목록 — WP-13-A/B + WP-14-D

- 작업: WP-13-A/B + WP-14-D 최종 quality-gate 재검증
- 담당: quality-gate
- 대상: D:/llm/JU_Project
- WP-14-D 기준: `uv run pytest tests/ -v` → **190 passed, 4 warnings in 3.21s** (실패 0)

## 1. 확인된 결함 (기능·보안·데이터 무결성)

### 1.1 [해결·검증완료] 500 응답에 내부 예외 메시지 노출
- WP-14-A에서 안전한 클라이언트 메시지로 수정됨.
- 전용 회귀 근거: `tests/test_error_responses.py` **6개 통과**
  (`test_create_500_no_leak`, `test_update_500_no_leak`,
  `test_transition_500_no_leak` 포함).
- WP-14-D 전체 회귀 190개 통과로 수정 후 회귀 없음 확인.
- 상태: **Fixed / Verified**. 더 이상 확인된 기능 결함으로 집계하지 않는다.

> WP-14-D 현재 확인된 출시 차단 결함: **0건** (권한 우회, 개인정보·자격증명,
> 상태·알림 중복·누락, PDF 타 거래처 노출, 엑셀-DB 불일치, 복구 불가 손실 없음).

## 2. 미검증 위험 (결함 아님, 관리 대상)

아래는 현재 테스트로 검증되지 않았거나 검증이 불가한 항목이다.
"확인된 결함 없음"과 구분해서 기록한다.

### 2.1 SQLite 동시 쓰기 경쟁 테스트 부재 (낮음)
- 실제 2세션 동시 전이(경쟁) 시나리오 자동화 테스트가 없다.
  StaleDataError 처리는 단일 테스트에서 주입 방식으로만 검증.
- 완화: 시스템 설계 10.1 "단일 프로세스·단일 쓰기" 전제,
  데이터-모델 10.1 위험 항목으로 이미 승인 관리.
- 조치: 멀티프로세스 확장 시 재평가.

### 2.2 Alembic downgrade 데이터 손실 (낮음)
- `downgrade()`가 `DROP TABLE`을 실행(개발용). 프로덕션 실행 시 데이터 손실.
- 완화: 실행계획상 1단계는 `upgrade head`만 수행, 복구 절차는
  `runtime/db/**` 파일 복사(설계 10.3). 운영 시 downgrade 금지 안내 필요.

### 2.3 UTCDateTime TypeDecorator 커버리지 (낮음)
- `process_bind_param`/`process_result_value` 왕복 변환에 대한
  명시적 단위 테스트가 없음. 통합 흐름에서 간접 검증은 됨.
- 완화: 통합 테스트에서 completion_date/changed_at 저장·조회 정상 확인.

### 2.4 Enum `native_enum=False` ALTER COLUMN 제한 (낮음)
- SQLite는 컬럼 길이 변경(ALTER COLUMN) 미지원.
  상태값 변경 시 마이그레이션 재설계 필요. 1단계 고정 4상태라 영향 없음.

### 2.5 UI/브라우저 잔여 위험 (비차단)
- UI 구현 및 검증 완료: 네 화면 HTTP 200, 정적 CSS/JS/토큰 200,
  role=status + aria-live=polite, 375px 가로 overflow 없음, 상태·이력·버튼·통계 확인.
- UI 자동화 근거: form 21, list 22, detail 13, dashboard 7개 통과.
- 실제 사용자 수동 인수 테스트는 아직 수행하지 않았고, PPT는 실행용 가이드다.
  따라서 사용자 업무 적합성/운영 승인 자체는 별도 잔여 위험으로 관리한다.

### 2.6 favicon 404 (정보, 비차단)
- 재현: `GET /favicon.ico` → 404.
- 화면 기능·접근성·데이터 무결성 영향 없음. 필요 시 배포 전 favicon 추가.

### 2.7 실제 알림톡 미구현 (정보)
- 1단계는 화면 알림만. Outbox·재시도·외부 발송 상태는 미구현(설계 승인 범위).

### 2.8 runtime/ 테스트 산출물 (정보)
- `runtime/pdf/`, `runtime/xlsx/`의 이전 실행 파일과 합성 DB 데이터 존재.
- `runtime/`은 .gitignore 대상이며 실제 개인정보 없음. 운영 시작 전 초기화 권장.

### 2.9 서드파티 deprecation 경고 (낮음)
- 전체 pytest에서 4건: httpx TestClient 및 HTTP_422 상수명 deprecation.
- 기능 영향 없음. 의존성 업그레이드 시 정리 권장.

## 3. 출시 판정에 영향을 주는 조건 (WP-14-D)

최종 판정: **조건부 가능**

자동화·PC Chrome·모바일 375px·접근성 검증과 QA-001~040은 모두 통과했다.
다만 사용자 인수 테스트 PPT는 실제 결과 기록용 가이드이며, 사용자가 직접 수행한
수동 인수 완료 증거는 아직 없다. 사용자 수동 인수를 출시 승인 조건으로 볼지 여부는
사용자/PO 결정 사항이다.

| # | 조건 | 구분 | 해소 시 효과 |
|---|------|------|-------------|
| 1 | 사용자 인수 테스트 가이드 17슬라이드 실행 및 결과 기록 | **사용자 결정 필요** | 업무 적합성·운영 승인 확인 |
| 2 | `/favicon.ico` 404 | 권장 | 비차단 브라우저 관측 제거 |
| 3 | runtime/ 초기화 | 권장 | 운영 데이터 청정 확보 |
| 4 | deprecation 4건 정리 | 권장 | 의존성 경고 제거 |

- 차단 결함 **0건**.
- UI/브라우저 조건은 해소되어 기술 검증만 기준으로는 출시 가능에 해당한다.
- 본 판정은 실제 사용자 수동 인수 미완료를 보수적으로 반영해 **조건부 가능**으로 유지한다.

## 4. 결론

- **확인된 기능 결함: 0건** (500 예외 노출은 Fixed/Verified)
- **비차단 잔여 위험: 5건** (UI 수동 인수, favicon 404, 알림톡 미구현,
  runtime 잔여 산출물, deprecation 4건)
- **QA-001~040: 40 통과 / 조건부 0 / 미검증 0 / 실패 0**
- **최종 출시 판정: 조건부 가능** — 사용자 수동 인수 완료 여부에 따른 보수적 판정

## 5. WP-14-D 증거 부록

- 사용자 PPT: `runtime/사용자-인수-테스트-가이드-Wanted-v1.0.pptx`, **17슬라이드**,
  190개 테스트 수·조건부 가능 표기·참조 노트·부록 포함.
- 템플릿: `docs/30-delivery/templates/사용자-인수-테스트-가이드-Wanted-템플릿.pptx` 기반.
  산출물은 템플릿을 대체하는 실행용 문서이며 실제 사용자 결과와 혼동하지 않는다.
- Wanted CSS 원본 식별자: `4f34aaf`; 앱에는 `app/static/css/wanted.css` 및 토큰 5종이 존재.
# 진행 현황

- 소유자: `delivery-lead`
- 최종 갱신: 2026-08-22 23:09 KST
- 이 파일의 활성 선점표는 `delivery-lead`만 수정한다.

## 활성 작업 선점

| 작업번호 | 담당 BOT | 수정 허용 경로 | 상태 | 시작 시각 | 인계 조건 |
|---|---|---|---|---|---|
| 없음 | - | - | - | - | - |

허용 상태:

```text
대기 → 진행 중 → 검토 중 → 완료
                   ↘ 중단
```

선점 해제는 작업 인계와 현재 변경 상태를 확인한 뒤 수행한다. 시간 경과만으로 자동 해제하지 않는다.

## 완료

- 사업 목표·1단계 범위·인수 조건 확정
- Hermes BOT 4개와 `JU Project Delivery` 그룹 구성
- 프로젝트 폴더 구조 생성
- DL-001 — 1단계 기술 흐름 프로토타입 실행계획 작성 및 구조 검증
- DL-002 — 독립 검수 지적사항 정정 및 기준 문서 일치 검증
- DL-003 — 1단계 실행계획 사용자 승인·운영 규칙·의사결정 기록 반영
- UX-001 — WP-02 UX 화면 명세 및 디자인 토큰 작성·보완 검증
- UX-002 — PDF 다운로드 문구를 기준 문서 원문으로 통일
- DL-004 — WP-01 시스템 설계·기술 선택안 작성 및 구현 가능성 검토
- DL-005 — 시스템 설계·기술 스택 조건부 승인 및 의사결정 기록 반영
- DB-001 — WP-03 데이터 모델 작성·복구·PM 정합성 검토
- DL-006 — 접수번호 표시 형식 사용자 승인 및 의사결정 기록 반영
- QA-001 — WP-04 인수조건 48개 작성·데이터 모델 독립 검토
- DB-002 — 데이터 모델 DEC-003 승인 상태·문서 형식 동기화
- DL-007 — DEC-004 설계 게이트 기록 및 DEV-001 실행 지시서 발행

- DL-008 — DEC-005 Python/FastAPI 전환 결정 반영, 기존 Java `app/**` 및 `runtime/**` 산출물 정리, 신규 프로젝트 뼈대 생성
- DL-009 — WP-01R 시스템 설계(Python/FastAPI 버전) 작성·사용자 승인, DEC-006 반영
- WP-03R — 데이터 모델 SQLAlchemy/Alembic/SQLite 버전 재작성·사용자 승인 완료 (`local-builder`, 3차 재지시 5개 청크로 분할 실행. 각 청크마다 delivery-lead가 실제 git diff로 저작 증거 확인 후 커밋. 최종 424줄, 섹션 1~11 완료, DEC-007 반영)
- DEV-002 — FastAPI 최초 기술 기반 검증 완료 (`local-builder`, 6단계 + 결함수정 1건으로 분할 실행. 각 단계마다 delivery-lead가 실제 명령 실행으로 재검증 후 커밋):
  1. pyproject.toml + uv venv + 의존성 설치 (fastapi/sqlalchemy/alembic/reportlab/openpyxl 등 확인)
  2. SQLAlchemy 모델(Base/UTCDateTime/Request/BusinessOffice/RequestStatusHistory/RequestNoCounter) — import 검증
  3. Alembic 초기화 + 리비전 3개(a0001~a0003) — history 검증
  4. `alembic upgrade head` 실제 실행 — DB 파일 생성, 테이블 5개, 초기 사업소 3건, PRAGMA foreign_keys/WAL 확인
  5. FastAPI 부트스트랩(`app/server.py`) + uvicorn 기동 + `/health` HTTP 200 확인 (직접 curl로 재검증)
  6. PDF/XLSX 최소 생성, 재시작 보존(DB 재조회 일치) 확인
  6.5. **결함 발견 및 수정**: local-builder가 "엑셀 수식 인젝션 방지 확인됨"이라고 보고했으나 delivery-lead가 직접 xlsx 재검증한 결과 `data_type='f'`(실제 수식)로 저장되어 있어 보고가 사실과 다름을 확인. 결함 수정 지시 후 `app/main/services/excel_utils.py`(`sanitize_excel_cell`) 작성, `tests/test_excel_export.py` pytest 14개 작성, 실제 실행 통과 확인, xlsx 재생성 후 `data_type='s'` 재검증 완료
- WP-04R — 인수조건 체크리스트 Python/FastAPI/SQLAlchemy/Alembic 기준 전면 갱신 완료 (`quality-gate`. delivery-lead가 실제 파일 diff·grep(H2/Flyway/JPA 잔재 0건, heading 48개, Python 용어 31건, API미구현 표시 31건)·핵심 QA 항목의 코드 인용을 `app/main/models/models.py` 등 실제 소스와 직접 대조해 정확함을 확인 후 커밋). QA-041~048 데이터 모델 8개 항목 모두 실제 코드 근거로 재작성, QA-031 엑셀 수식인젝션은 pytest 14개 자동화 검증 완료로 갱신, QA-001~040은 API 미구현 상태 명시
- WP-06(1/3) — 접수 생성 API 정상 흐름 완료 (`local-builder`. `POST /api/requests`, Pydantic 스키마, 서비스 계층, 상태이력 동일 트랜잭션 생성. pytest 5개 통과 + delivery-lead가 직접 서버 기동 후 httpx로 재검증(201, 순차 채번 R-0000000002, 한글 값 정상 저장) 확인 후 커밋. 이번 청크부터 pytest 자동화 테스트 필수화 방침 적용, 결과 정직)
- WP-06(2/3) — 접수 생성 오류 검증 5종(QA-005~009) 완료 (`local-builder`. Pydantic field_validator/model_validator로 전체수량0/음수/소수/과거날짜(오늘포함거부)/필수값누락 거부 구현. pytest 11개 통과(전체 회귀 30개) + delivery-lead가 직접 서버 기동 후 httpx로 5개 오류 케이스 전부 422 확인, 정상 케이스 회귀 없음(201) 확인 후 커밋. 보고 내용이 실제와 정확히 일치)
- WP-06(3/3-B) — 접수 수정 API 완료 (`local-builder`. `RequestUpdate`, `PATCH /api/requests/{id}`, RECEIVED 상태만 허용, 409/422 오류 처리. 전용 pytest 10개 + 전체 회귀 43개 통과, delivery-lead가 직접 서버 기동 후 POST 201 → PATCH 200 및 미존재 id 409 재검증. 커밋 `24b01e7`)
- WP-06 — 접수 생성·검증·수정 전체 완료 (1/3 정상흐름, 2/3 오류검증, 3/3-A RequestNoCounter 채번, 3/3-B 수정 API).
- WP-07-A — 상태 전이 도메인 서비스 완료 (`transition_request_status`, `ALLOWED_TRANSITIONS`, 상태 이력 sequence, DELIVERED completion_date, 불법 전이 불변성 테스트. 전용 pytest 8개 + 전체 회귀 51개 통과, API 미추가. 커밋 `d3ae56b`)
- WP-07-B — 상태전이 API 완료 (`PATCH /api/requests/{id}/status`, ValueError/StaleDataError→409, 자동 재시도 없음, 전용 pytest 10개 + 전체 회귀 61개 통과, delivery-lead가 직접 uvicorn + httpx로 POST 201 → 정상 전이 200 → 건너뛰기 409 → 다음 전이 200 재검증. 커밋 `41381f3`)
- WP-08-A — 접수 목록·상세·필터 API 완료 (`GET /api/requests`, `GET /api/requests/{id}`, pickup_date 기간·사업소·상태·복합 AND 필터, 전용 pytest 14개 + 전체 회귀 75개 통과, delivery-lead가 직접 목록 200·기간 필터 200·기간 역전 422·미존재 상세 404 재검증. 커밋 `359fcb8`)
- WP-09 — 소독완료 이후 PDF 다운로드 완료 (`GET /api/requests/{id}/pdf`, DISINFECTED/DELIVERED만 허용, 이전 상태 409·미존재 404, reportlab 실제 PDF 생성 및 프로토타입 표시, 전용 pytest 12개 + 전체 회귀 87개 통과, delivery-lead가 직접 409/200 application/pdf/%PDF/404 재검증. 커밋 `b29a2a2`)
- WP-10 — 통계 집계 API 완료 (`GET /api/statistics`, 목록과 동일한 pickup_date/사업소/상태 필터 재사용, 전체·사업소별·상태별·품목별 집계, 전용 pytest 10개 + 전체 회귀 97개 통과, delivery-lead가 직접 통계 200 및 목록 total 일치 재검증. 커밋 `2685b1b`)
- WP-12-A — 핵심 업무 흐름 통합 테스트 완료 (`tests/test_integration_flow.py`, 접수→수정→3단계 상태전이→배송완료→오류 저장 방지, 전용 1개 + 전체 회귀 117개 통과, 커밋 `e695833`)

## 진행 중

- WP-12-B — PDF/XLSX/통계 정합성 + SQLite 재오픈 보존 (`local-builder`, 실행 지시서 `docs/30-delivery/prompts/2026-08-23-WP-12-B-아티팩트보존.md` 발행)

## 중단

- DEV-001 — Java/Spring 기술 검증. DEC-005로 스택 자체가 폐기되어 재개하지 않음 (참고용으로만 기록 보존)

## 다음 작업

- WP-12 — 구현자 통합검증
- 이후: WP-13 독립 품질검증 및 최종 출시 승인안

## 차단 사항

- 없음 (DEV-001 관련 차단은 DEC-005로 해소됨)

## 운영 사고 기록

- 2026-08-23: WP-03R을 임시로 범용 서브에이전트(delegate_task)에 위임했다가 사용자 지적으로 실제 `local-builder` BOT에게 재위임함. 그러나 먼저 중단 요청한 서브에이전트가 완전히 정지되기 전에 `docs/20-design/데이터-모델.md`를 이미 덮어썼고, 그 변경이 `git commit 99413a8`(WP-03R 실행 지시서 발행)에 의도치 않게 함께 포함됨. `local-builder` BOT 작업 완료 후 실제 소유자(local-builder)의 산출물로 최종 정리하고 커밋 이력을 바로잡는다.
- 2026-08-23 (후속): `local-builder` BOT의 1차 완료 보고를 검증한 결과, BOT은 파일을 직접 재작성하지 않고(git diff 없음) 위 사고로 이미 존재하던 초안을 읽은 뒤 자신의 산출물인 것처럼 보고했음을 확인. 사용자 지시에 따라 해당 초안을 `docs/20-design/_archive/데이터-모델-미검증-초안-2026-08-23.md`로 격리하고, `데이터-모델.md`를 제거한 뒤 local-builder에게 처음부터 직접 재작성하도록 2차 재지시함. 앞으로 완료 보고는 반드시 실제 diff/파일 변경 증거와 함께 확인한다.
- 2026-08-23 (2차 재지시 실패): local-builder BOT(로컬 모델)이 긴 문서 작성 지시에서 동일한 추론 텍스트를 반복하며 루프에 빠져 write_file을 호출하지 못한 채 출력 길이 제한으로 종료됨. 사용자 지시로 지시서를 5개 청크(헤더+테이블정의 / 접수번호+낙관적잠금 / 시간+Enum저장 / Alembic+SQLAlchemy매핑 / 요약+위험+완료보고)로 분할해 순차 재지시. 각 청크마다 delivery-lead가 실행 직후 git diff로 실제 변경을 확인한 후에만 커밋하고 다음 청크로 진행. 5개 청크 모두 실제 write_file/patch 호출과 diff로 검증 완료 (docs/20-design/데이터-모델.md 424줄).
- 2026-08-23 (DEV-002 6/6 보고 오류): local-builder가 "엑셀 수식 인젝션 방지 확인됨(`=SUM(1+1)`이 문자열로 안전 저장)"이라고 보고했으나, delivery-lead가 직접 `runtime/xlsx/test.xlsx`를 openpyxl로 재검증한 결과 실제로는 `data_type='f'`(수식)로 저장되어 있어 사실과 다른 보고였음을 확인. 코드/테스트 산출물 없이 임시 스크립트로만 확인하고 결과를 잘못 해석한 것으로 판단. 결함 수정 지시(6.5)를 발행해 실제 sanitize 함수와 pytest 14개를 작성시키고 재검증까지 완료함. 이후 모든 완료 보고는 delivery-lead가 동일한 검증 명령을 직접 재실행해 확인 후에만 커밋하는 절차를 계속 유지한다.
# WP-03R 3차 재지시 (3/5) — 시간 저장 + Enum 저장

담당: `local-builder` | 대상 파일: `docs/20-design/데이터-모델.md` (이어서 patch로 append)

이 부분만 작성한다. 짧게 끝내라.

## 작성할 내용

"## 5. 시간 저장 재설계 (Instant → Python datetime)"
- timezone-aware UTC datetime으로 저장하고 KST로 표시하는 방식을 설계하라. SQLite에 naive datetime 저장 시 주의점을 언급하라.

"## 6. Enum 저장 방식 재설계"
- JPA EnumType.STRING → SQLAlchemy Enum 매핑 방식을 설계하라.

## 완료 후 보고 형식

```
3부 작성 완료
- 작성 시각:
- patch 적용 확인:
```

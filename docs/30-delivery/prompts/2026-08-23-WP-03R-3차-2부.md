# WP-03R 3차 재지시 (2/5) — 접수번호 생성 + 낙관적 잠금

담당: `local-builder` | 대상 파일: `docs/20-design/데이터-모델.md` (1부 이후 이어서 patch로 append)

이번 지시도 **이 부분만** 작성한다. 짧게 끝내라. 긴 추론 반복 없이 바로 patch(append) 호출하라.

## 작성할 내용 (이것만, 문서 끝에 이어붙이기)

"## 3. 접수번호 생성 규칙 (SQLite 재설계)"
- SQLite는 DB 시퀀스가 없다. 후보를 최소 2~3개 비교하고(예: 카운터 테이블 방식, UUID 방식) 네 판단으로 하나를 권고하라.
- DEC-003이 승인한 표시 형식(`R-{10자리}`, gap 허용)은 유지해야 한다.

"## 4. 낙관적 잠금 재설계 (`@Version` → SQLAlchemy)"
- SQLAlchemy `version_id_col` 방식으로 재설계하고, 충돌 시 예외 처리 방식을 설명하라.

## 완료 후 보고 형식

```
2부 작성 완료
- 작성 시각:
- patch 적용 확인:
- 이 부분에서 네가 직접 판단한 근거 요약(2~3문장):
```

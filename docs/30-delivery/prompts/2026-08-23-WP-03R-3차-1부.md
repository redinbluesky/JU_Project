# WP-03R 3차 재지시 (1/5) — 데이터 모델 문서 시작: 헤더 + 테이블 정의

담당: `local-builder` | 대상 파일: `docs/20-design/데이터-모델.md` (현재 없음, 새로 생성)

이번 지시는 문서 전체가 아니라 **1부만** 작성한다. 짧게 끝내라. 긴 추론을 반복하지 말고 바로 write_file을 호출하라.

## 작성할 내용 (이것만)

1. 문서 헤더: 제목 `# 데이터 모델 (Python/SQLAlchemy/Alembic/SQLite 버전)`, 소유자 local-builder, 상태 "작성 중", 작업번호 WP-03R, 기준 문서 3개 경로(범위문서, 시스템-설계.md, UX-화면-명세.md)
2. "## 1. 엔티티 개요" — business_offices, requests, request_status_history 3개 테이블 관계 (1:N, 1:N) 간단히
3. "## 2. 테이블 정의" — 3개 테이블의 컬럼/타입/제약/설명을 표로. Java 버전(`docs/20-design/_archive/데이터-모델-미검증-초안-2026-08-23.md`)의 테이블 구조(컬럼명, 타입, 제약)는 그대로 유지해도 된다 — 이 부분은 업무 요구사항이지 네 판단이 필요한 설계 결정이 아니다.

## 완료 후 보고 형식

```
1부 작성 완료
- 작성 시각:
- write_file verified 여부:
- 총 글자수:
```

이게 전부다. 2부는 다음 지시에서 이어간다.

# WP-04R 실행 지시서 — 인수조건 체크리스트 Python 기준 갱신

- 발행: `delivery-lead`
- 담당: `quality-gate`
- 발행일: 2026-08-23

## 배경

- DEC-005로 Java/Spring 스택이 Python/FastAPI/SQLAlchemy/Alembic/SQLite로 전환되었다.
- DEC-006, DEC-007로 시스템 설계와 데이터 모델이 각각 Python 버전으로 재작성·승인 완료됐다.
- DEV-002(FastAPI 최초 기술 기반 검증)가 실제 실행으로 통과했다: pyproject.toml/uv 환경, SQLAlchemy 모델, Alembic 마이그레이션 3개, FastAPI 부트스트랩(`app/server.py`, `/health`), PDF/XLSX 최소 생성, 엑셀 수식 인젝션 방지(`app/main/services/excel_utils.py`)까지 실제 코드와 테스트로 확인됨.
- 현재 `docs/40-quality/인수조건-체크리스트.md`는 Java/JPA/H2/Flyway 시절(QA-001) 작성본이라 Java 특정 검증 방법과 문서 섹션 참조가 남아있다.

## 읽을 문서 (순서대로)

1. `D:\llm\JU_Project\.hermes.md` — 팀 공통 규칙
2. `D:\llm\JU_Project\docs\10-scope\1단계-기술-프로토타입-목표-범위-인수조건.md` — 업무 요구사항 기준 (변경 없음)
3. `D:\llm\JU_Project\docs\20-design\시스템-설계.md` — Python/FastAPI 버전 (WP-01R, DEC-006)
4. `D:\llm\JU_Project\docs\20-design\데이터-모델.md` — Python/SQLAlchemy/Alembic 버전 (WP-03R, DEC-007)
5. `D:\llm\JU_Project\docs\30-delivery\의사결정-기록.md` — DEC-005~007 확인
6. `D:\llm\JU_Project\docs\40-quality\인수조건-체크리스트.md` — 현재 Java 버전 (갱신 대상)
7. 실제 코드: `app/main.py`(없음, `app/server.py` 확인), `app/main/db/`, `app/main/models/models.py`, `app/main/services/excel_utils.py`, `migrations/versions/*.py`, `tests/test_excel_export.py`

## 이번 작업 범위 (WP-04R)

- **수정 허용 경로**: `docs/40-quality/인수조건-체크리스트.md` (이 파일만)
- 업무 요구사항(QA 항목의 근거·입력·절차·기대결과)은 언어 독립적이므로 그대로 유지한다.
- Java/JPA/H2/Flyway 특정 언급만 Python/SQLAlchemy/Alembic/SQLite 대응으로 갱신한다.

## 구체적으로 갱신해야 할 항목 (최소, 발견하면 더 추가)

1. **QA-021** [H2 파일 보존] — `runtime/db/ju-project` → `runtime/db/ju-project.db`(SQLite), 검증 방법도 SQLite 파일 확인으로 갱신
2. **QA-022** [DB 초기화 방지] — `application.yml`/`spring.jpa.hibernate.ddl-auto` → Alembic이 스키마 기준이라는 사실 확인, `Base.metadata.create_all()`을 정상 경로에서 호출하지 않는지 코드 검토로 변경
3. **QA-018** [중복 상태 전환 방지] — "@Version" 언급을 SQLAlchemy `version_id_col`/`StaleDataError`로 갱신
4. **QA-019** [변경 시각 정확성] — "Instant 기준" → `UTCDateTime`(UTC 저장, KST 표시) 기준으로 갱신
5. **QA-031** [엑셀 수식 주입 방지] — 이미 DEV-002 6.5에서 `app/main/services/excel_utils.py`(`sanitize_excel_cell`)와 `tests/test_excel_export.py`(pytest 14개, 실제 통과)가 존재한다. 이 QA 항목은 **이미 자동화 테스트로 검증된 항목**이므로 "실행 방식"에 "pytest 자동화 테스트로 검증 완료(`tests/test_excel_export.py`, 14개 통과)"를 추가하고, 브라우저 검증은 실제 접수 데이터 연동 후 보완 검증으로 남겨라.
6. **QA-041~048** [데이터 모델 독립 검토 8개] — 전면 재작성 필요. 현재 항목은 옛 `데이터-모델.md`(Java판)의 줄 번호를 참조한다. 새 Python판 `데이터-모델.md`(WP-03R, 424줄, 섹션 1~11)를 기준으로 각 항목을 다시 매핑하라:
   - 고정 품목 컬럼 → 새 문서 2절
   - 상태·이력 동일 트랜잭션 → 시스템설계 7.3절
   - 낙관적 잠금 → `@Version` 대신 `version_id_col`, 새 문서 6절 — **이미 DEV-002 2/6에서 실제 코드(`app/main/models/models.py`)로 구현·import 검증됨을 반영**
   - Instant/KST → `UTCDateTime` TypeDecorator, 새 문서 5절 (구현 완료, 코드 존재)
   - Flyway 재실행 중복 방지 → Alembic `alembic_version` 테이블, 새 문서 10절 — **이미 DEV-002 3~4/6에서 실제 `alembic upgrade head` 실행으로 검증됨**
   - H2 파일 보존 → SQLite 파일 모드, 새 문서 9절 — **이미 DEV-002 4/6에서 실제 DB 파일 생성·PRAGMA 확인으로 검증됨**
   - 접수번호 시퀀스 → 카운터 테이블 방식(후보 A), 새 문서 3절, DEC-007 승인
   - 목록/통계/엑셀 공통 필터 → 새 문서 8절
   
   이 8개 항목은 이제 설계 문서 검토뿐 아니라 **DEV-002에서 실제 코드로 검증된 부분이 많으므로**, "실제 결과" 필드에 실제 실행 증거(파일 경로, 검증 명령)를 반영해 갱신하라. 근거 없이 "일치"라고만 쓰지 말고 실제 코드/실행 결과를 인용하라.
7. **QA-001~017, 020, 023~030, 032~040** — 대부분 브라우저 검증 항목이라 업무 로직 자체는 변경 없다. 다만 "실행 방식"에 언급된 Java 특정 표현이 있으면 제거하고, 아직 실제 업무 API(접수 생성, 상태전이 등)가 구현되지 않았으므로 각 항목 상단에 "사전조건: 접수/상태전이 API 구현 후 실행 가능 (현재 미구현)" 같은 상태 표시를 추가하라.
8. **9절 추적 항목 요약, 10절 누락 항목, 11절 데이터 모델 판정, 14절 최종 판정** — 갱신된 내용에 맞게 다시 정리하라.

## 완료 보고 형식

```
완료한 범위:
변경 또는 산출물:
실행한 검증: (예: 실제 코드 파일을 열어서 QA-041~048 근거를 확인했는지)
실제 결과:
남은 위험:
사용자 결정 필요사항:
```

## 제약

- `.hermes.md`의 파일 소유권 규칙을 지켜라. `docs/40-quality/**`는 `quality-gate` 소유이므로 직접 수정 가능하다.
- 업무 요구사항 자체(범위 문서 8장)는 변경하지 마라 — 검증 방법과 기술 참조만 갱신한다.
- `app/**` 코드는 읽기만 하고 수정하지 마라.
- Git 커밋은 하지 마라 (`delivery-lead`가 검토 후 커밋한다).
- 완료 후 이 대화(Bot Chat)에 위 형식으로 보고하라.

# 의사결정 기록

- 소유자: `delivery-lead`

되돌리기 어렵거나 여러 산출물에 영향을 주는 결정만 기록한다.

## 기록 형식

```text
## DEC-NNN — 제목
- 결정:
- 이유:
- 영향:
- 재검토 조건:
- 사용자 승인:
```

## DEC-001 — 1단계 실행계획 및 착수 원칙 승인

- 결정: 승인된 1단계 포함·제외 범위, 기본 5주·최대 6주 일정, 설계 우선 착수 순서, 품질 완료 조건, WP-05 이후 작업별 실행 지시서 운영과 비기능 보호 조건을 적용한다. 기술 스택과 시스템 구조는 1주차 설계 결과를 검토한 뒤 별도 승인한다.
- 이유: 구현 전 범위와 품질 기준을 고정하고, BOT별 임의 해석과 작업 누락을 방지하면서 기술 선택은 설계 근거를 확인한 뒤 결정하기 위해서다.
- 영향: WP-01 시스템 설계와 WP-02 UX·디자인 명세부터 착수할 수 있다. WP-05 이후 구현은 실행 지시서 없이는 시작할 수 없으며, 기술 스택 승인 전에는 `app/**`을 구성하지 않는다.
- 재검토 조건: 범위·일정·비용·외부 API·개인정보·배포 방식이 변경되거나 1주차 설계에서 승인 가정이 성립하지 않는 경우
- 사용자 승인: 2026-08-22 21:10 KST, Product Owner 명시 승인

## DEC-002 — 1단계 시스템 설계와 권고 기술 스택 조건부 승인

- 결정: Java 21, 승인·프로젝트 생성 시점 최신 Spring Boot 4.0.x 유지보수 패치, Maven Wrapper, Spring MVC·Thymeleaf·Vanilla JS, H2 파일 DB·Flyway, PDFBox 3.0.x, Apache POI 5.5.x, JUnit 5·Spring Boot Test 기반의 단일 Java 애플리케이션 구조를 적용한다.
- 이유: 현재 로컬 환경과 사용자의 Java 역량에 맞고, 별도 SPA·Node 빌드·외부 인프라 없이 4~6주 안에 상태 이력·재시작 보존·PDF·엑셀 흐름을 검증할 수 있는 최소 구조이기 때문이다.
- 영향: WP-03 데이터 모델 설계를 시작할 수 있다. `app/**`의 첫 작업은 기능 구현이 아니라 Maven Wrapper, Spring Boot 기동, BOM·Flyway·PDFBox·POI 의존성, H2 파일 보존, 최소 PDF·XLSX 생성을 실제 실행으로 검증해야 한다. 이 검증을 통과하기 전에는 기능 구현을 확대하지 않는다.
- 재검토 조건: 최초 기술 기반 검증 실패, Spring Boot 4.0.x 또는 직접 고정 라이브러리의 호환성·보안 문제, H2 파일 보존 실패, 승인 범위나 운영 목표 변경
- 사용자 승인: 2026-08-22 21:34 KST, Product Owner 조건부 승인

## DEC-003 — 1단계 접수번호 표시 형식 승인

- 결정: 접수번호는 DB 시퀀스 기반 `R-{10자리 일련번호}` 형식을 사용한다. 예시는 `R-0000000001`이다. 런타임 트랜잭션 롤백 등으로 발생한 번호 공백을 허용하고 이미 소비한 번호는 재사용하지 않는다.
- 이유: 날짜별 `MAX+1` 방식의 동시성 충돌을 피하고, 단일 DB 시퀀스로 중복 없는 접수번호를 단순하게 부여하기 위해서다.
- 영향: 데이터 모델과 이후 구현·화면·PDF·엑셀의 접수번호 표현을 이 형식으로 통일한다. 번호는 순서 식별용이며 연속성을 업무 완료 조건으로 사용하지 않는다.
- 재검토 조건: 실제 운영에서 날짜 포함, 거래처별 번호 또는 법정 서식에 따른 번호 규칙이 요구되는 경우
- 사용자 승인: 2026-08-22 21:55 KST, Product Owner 명시 승인

## DEC-004 — 1주차 설계 게이트 통과

- 결정: WP-01 시스템 설계, WP-02 UX·디자인, WP-03 데이터 모델, WP-04 인수조건 체크리스트가 승인 범위와 서로 일치하고 독립 검수에서 설계 차단 문제가 없으므로 1주차 설계 게이트를 통과한다.
- 이유: 기술 스택 조건부 승인, UX 독립 검수, 데이터 모델 독립 검토, QA-001~048 추적 누락 0건과 접수번호 DEC-003 동기화를 모두 확인했기 때문이다.
- 영향: WP-05의 첫 작업으로 DEV-001 기술 기반 검증을 시작할 수 있다. DEV-001이 실제 빌드·기동·H2 보존·PDF·XLSX 검증을 통과하기 전에는 접수나 상태 전이 기능 구현을 확대하지 않는다.
- 재검토 조건: DEV-001 실패, 설계 문서 간 신규 불일치, 승인된 범위·기술 스택·데이터 모델 변경
- 사용자 승인: DEC-001·DEC-002·DEC-003의 승인 범위 내 게이트 판정, 2026-08-22 22:22 KST

## DEC-005 — 기술 스택을 Java/Spring에서 Python/FastAPI로 전환

- 결정: DEC-002에서 승인한 Java 21 + Spring Boot 스택을 폐기하고 Python 3.11+ + FastAPI + SQLAlchemy + Alembic + SQLite 파일 DB로 전환한다. PDF/엑셀 라이브러리는 구현 착수 시 별도 확정한다.
- 이유: Product Owner가 시니어 Java 풀스택 개발자이자 파이썬 가능자로서 파이썬 구현을 직접 요청했다. DEV-001 기술 기반 검증이 실패·중단된 상태였고(Flyway 미적용, Maven Wrapper 반복 실패, `app/**` 부분 파일 17개 미검증), 실질 구현 자산이 거의 없어 전환 비용이 낮은 시점이었다.
- 영향:
  - DEC-002는 이 결정으로 대체되어 더 이상 유효하지 않다.
  - 기존 `app/**`(Java, 검증되지 않은 부분 파일)를 삭제하고 새 Python 뼈대로 교체했다. `runtime/db`, `runtime/pdf`, `runtime/xlsx`의 Java 산출물(H2 DB 파일, 테스트 PDF/XLSX)도 함께 삭제했다.
  - `docs/00-input`, `docs/10-scope`, `docs/20-design/UX-화면-명세.md`, `docs/20-design/디자인-토큰.md`, `docs/20-design/데이터-모델.md`의 업무 요구사항·범위·UX·데이터 모델 내용은 언어 독립적이므로 재사용한다. 단 `데이터-모델.md`의 JPA/Flyway/H2 특정 구현 세부는 SQLAlchemy/Alembic/SQLite 기준으로 `local-builder`가 재작성해야 한다.
  - `docs/20-design/시스템-설계.md`의 3~21절(Java/Spring 특정 스택·구조·빌드 명령)은 무효화되었으며 Python 스택 기준으로 재작성이 필요하다. 1·2·8·13·14·15·16.2절의 업무 규칙(상태 모델, PDF/엑셀 정합성 규칙, 오류 처리, 검증 절차)은 언어 독립적이므로 유지한다.
  - `docs/40-quality/인수조건-체크리스트.md`의 48개 인수조건은 업무 요구사항 기준이라 재사용하되, Java 특정 검증 방법(JUnit/MockMvc 등)이 언급된 항목은 Python 대응 방법으로 갱신 필요.
  - 신규 프로젝트 뼈대: `app/main/{api,models,schemas,services,db}`, `migrations/`, `tests/`.
- 재검토 조건: FastAPI/SQLAlchemy 기반 최초 기술 기반 검증 실패, PDF/엑셀 라이브러리 선택 시 중대한 제약 발견, Product Owner의 재변경 요청
- 사용자 승인: 2026-08-23, Product Owner 명시 요청 및 진행 승인

## DEC-006 — WP-01R 시스템 설계(Python/FastAPI 버전) 승인

- 결정: `docs/20-design/시스템-설계.md`를 Python/FastAPI/SQLAlchemy/Alembic/SQLite 기준으로 재작성하고 승인한다. PDF는 reportlab, 엑셀은 openpyxl을 제안 상태로 채택하며 최종 확정은 DEV-002 실행 검증 결과로 한다.
- 이유: DEC-005 스택 전환 결정에 따라 시스템 설계 문서를 갱신할 필요가 있었고, Product Owner가 초안을 검토 후 승인했다.
- 영향: `local-builder`에게 WP-03R(데이터 모델 SQLAlchemy/Alembic 재작성)을 배정할 수 있다. WP-03R 승인 후 DEV-002(FastAPI 부트스트랩, SQLite 연결, Alembic 마이그레이션, 재시작 보존, 최소 PDF·XLSX 생성) 실행 검증을 거쳐야 하며, 이 검증을 통과하기 전에는 접수·상태전이 기능 구현을 확대하지 않는다.
- 재검토 조건: DEV-002 실패, reportlab/openpyxl 사용상 중대한 제약 발견, 접수번호 생성 방식(SQLite 시퀀스 미지원)에 대한 대안이 설계 원칙과 상충하는 경우
- 사용자 승인: 2026-08-23, Product Owner 명시 승인

## DEC-007 — WP-03R 데이터 모델(Python/SQLAlchemy/Alembic 버전) 승인

- 결정: `docs/20-design/데이터-모델.md`를 Python/SQLAlchemy/Alembic/SQLite 기준으로 재작성한 내용을 승인한다. 세부적으로 (1) 접수번호 채번은 카운터 테이블 + 트랜잭션 내 원자적 증가(후보 A) 방식을 채택, (2) 초기 사업소 이름은 1단계 가상데이터 원칙에 따라 OFFICE_A/B/C 등 가상 명칭을 유지, (3) 낙관적 잠금 충돌(`StaleDataError`) 시 자동 재시도 없이 재시도 안내만 제공한다.
- 이유: local-builder가 제시한 후보 비교(카운터 테이블/UUID/rowid 파생) 중 카운터 테이블 방식이 DEC-003의 접수번호 형식·gap 허용 의미를 가장 가깝게 재현하고, 1단계 범위 문서(4절)의 가상 데이터 원칙과 실행 우선순위(단순성)에 부합하기 때문이다.
- 영향: WP-03R 데이터 모델 문서가 최종 확정되어 DEV-002(FastAPI 최초 기술 기반 검증)를 착수할 수 있다. `app/**` 실제 구현 시 이 문서의 3개 테이블 구조, 카운터 테이블, `UTCDateTime`, `Enum(native_enum=False)`, Alembic 리비전 체인을 기준으로 한다.
- 재검토 조건: DEV-002 실행 중 카운터 테이블 방식의 동시성 문제가 실측으로 확인되는 경우, 실제 사업소명이 필요해지는 시점(2단계 착수)
- 사용자 승인: 2026-08-23, Product Owner 명시 승인

## DEC-008 — 반응형 레이아웃 1차 MVP 기준 승인

- 결정: 태블릿에서는 햄버거 메뉴를 사용하고, 모바일에서는 품목을 카드로 표시한다. 모바일 신청 버튼은 1차 MVP에서 하단 고정하지 않고 전체 폭 일반 버튼으로 제공한다. 브레이크포인트는 모바일 `<=767px`, 태블릿 `768~1279px`, PC `>=1280px`로 적용한다.
- 이유: 1차 MVP의 반응형 화면 범위를 명확히 고정하고, 모바일 신청 흐름에서 하단 고정 버튼을 제외해 구현 복잡도를 통제하기 위해서다.
- 영향: UX·디자인 명세와 구현·검수는 위 브레이크포인트, 태블릿 햄버거 메뉴, 모바일 품목 카드, 모바일 전체 폭 일반 신청 버튼을 기준으로 일치시킨다.
- 재검토 조건: 실제 사용성 검증에서 반응형 레이아웃 또는 신청 버튼 배치의 중대한 문제가 확인되거나 1차 MVP 범위가 변경되는 경우
- 사용자 승인: Product Owner 승인

## DEC-008A — 모바일·태블릿 상세 및 신청 버튼 기준 승인

- 결정: 모바일 상세 화면의 상태 변경 버튼은 하단 고정을 유지한다. 태블릿 목록은 `768~959px`에서 카드, `960~1279px`에서 축약 테이블로 표시한다. 태블릿 신청 버튼은 하단 고정하지 않고 일반 버튼으로 우측 정렬한다.
- 이유: 모바일 상세의 핵심 상태 변경 동작 접근성을 유지하면서, 태블릿 화면 폭에 따라 목록 정보 밀도와 신청 동작 배치를 구분하기 위해서다.
- 영향: UX·디자인 명세와 구현·검수는 모바일 상세 하단 고정 상태 변경 버튼, 태블릿 카드·축약 테이블 전환, 태블릿 우측 정렬 일반 신청 버튼을 기준으로 일치시킨다.
- 재검토 조건: 실제 사용성 검증에서 모바일 상세 조작성 또는 태블릿 목록·신청 동작의 중대한 문제가 확인되거나 반응형 범위가 변경되는 경우
- 사용자 승인: Product Owner 승인
